import React from 'react';

import Nav from './Nav/Nav';

const Header = () => {
    return (
        <header className="header row">
            <Nav />
        </header>
    )
}

export default Header;
