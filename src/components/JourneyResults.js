import React from 'react';

const JourneyResults = ({ results }) => {
    console.log({ results });
    return (
      <div>
        Journey results.

      </div>
    );
};

export default JourneyResults;